{
  "_comment_": "Drop this into ~/Library/Application Support/Claude/claude_desktop_config.json (merge if you have other MCP servers there). Then restart Claude Desktop. Make sure Claude.app itself has Full Disk Access in System Settings → Privacy & Security → Full Disk Access, otherwise the stdio MCP binary will not be able to read chat.db.",
  "mcpServers": {
    "imsg-relay": {
      "command": "/Applications/iMessage Relay.app/Contents/MacOS/ImsgRelay",
      "args": ["--mcp"]
    }
  }
}
